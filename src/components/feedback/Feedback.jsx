import classes from "./Feedback.module.css";

const Feedback = () => {
  return (
    <div className={classes.container}>
      <p className={classes.text}>Your feedback is very important to us</p>
    </div>
  );
};

export default Feedback;
