import classes from "./Title.module.css"

const Title = () => {
    return (
        <div className={classes.title}>
         <p className={classes.rate}>Please Rate</p>
        </div>
    )
}

export default Title;